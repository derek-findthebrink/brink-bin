#!/usr/bin/env coffee

program = require "commander"

program
	.option("-n, --name", "command name")
	.parse(process.argv)

input = {
	name: "INSERT NAME HERE"
}
if program.name
	input.name = program.name

template = require("./templates/global-command")(input.name)

console.log template
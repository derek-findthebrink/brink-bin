chai = require "chai"
chai.should()
expect = chai.expect

describe "program: brink-new", ->
	it "should create a directory of files"
	it "should request confirmation if the root directory isn't empty"